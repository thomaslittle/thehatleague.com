/**
 * Shared devops-application constants. Mirrors lib/captain.ts so the
 * server-side validation and the client-side counter stay in lockstep —
 * change the bound here and both sides update together.
 */

export const DEVOPS_PITCH_MIN = 40;
export const DEVOPS_PITCH_MAX = 1200;
