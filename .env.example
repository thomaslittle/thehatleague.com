# Public values (safe to ship to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://tcjizqwqqttdicqrgrmt.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_1VL8QgsnDhCnsA1rRSfptw_u7-Pkx5V
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only Discord bot creds (used by the clips + messaging adapters).
# Leave blank to keep the sample-clip fallback in lib/discord/clips.ts.
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=
DISCORD_CLIPS_CHANNEL_ID=

# Discord webhook URL — when set, new announcements get fan-out posted to
# the configured channel. Create it via the channel's Integrations > Webhooks.
DISCORD_ANNOUNCEMENTS_WEBHOOK_URL=

# (Optional) automated tracker-rank adapter. Leave blank in dev — the manual
# adapter is the live path; the automated one is stubbed.
TRACKER_SCRAPER_URL=
TRACKER_SCRAPER_TOKEN=

# (Optional) ballchasing.com group-stats adapter. Wire once a league
# ballchasing account exists and the Season 4 group ids are minted.
BALLCHASING_API_KEY=

# (Optional) Twitch Helix client credentials — when set, the nav shows a
# pulsing "LIVE" badge whenever hat_dad_gaming is streaming. Create the
# app at https://dev.twitch.tv/console (App Access Token flow).
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
