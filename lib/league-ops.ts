/**
 * Shared league-ops application constants. Mirrors lib/captain.ts — change
 * the bound here and both the server-side validator and the client-side
 * character counter pick it up.
 */

export const ADMIN_PITCH_MIN = 40;
export const ADMIN_PITCH_MAX = 1200;
